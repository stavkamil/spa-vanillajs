/**
 * Helper file for extra helper functions
 */

const helpers = {};

helpers.getPageTitleEl = () => {
  return document.getElementById("pageTitle");
};

helpers.getPageContentEl = () => {
  return document.getElementById("pageContent");
};
